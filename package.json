{
  "name": "internick-smart-alt-generator",
  "version": "1.3.2",
  "description": "Block editor integration for AI Alt Text Generator for Images.",
  "license": "GPL-2.0-or-later",
  "private": true,
  "scripts": {
    "build": "wp-scripts build --output-path=build",
    "start": "wp-scripts start --output-path=build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
